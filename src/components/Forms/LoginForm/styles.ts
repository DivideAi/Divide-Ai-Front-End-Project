import styled from 'styled-components';

export const StyledLoginForm = styled.form`
  display: flex;
  flex-direction: column;
  width: 320px;
  gap: 37px;
  margin: 50px auto;

  .link_style {
    font-size: 1rem;
    font-weight: 600;
    color: var(--grey-0);
    margin: 0 auto;
  }
`;
